import { Matcher, MatchResult } from '../../../assertions';
export declare function stringLike(pattern: string): StringLike;
export declare function sortByRunOrder(pattern: any[]): Matcher;
export declare function stringNoLongerThan(max: number): Matcher;
declare class StringLike extends Matcher {
    private readonly pattern;
    name: string;
    constructor(pattern: string);
    test(actual: any): MatchResult;
}
export {};
